// Define the famous person's name
let famousPerson: string = 'Albert Einstein';

// Define the famous quote
let famousQuote: string = 'A person who never made a mistake never tried anything new.';

// Compose the message
let message: string = `"${famousQuote}" - ${famousPerson}`;

// Print the message
console.log(message);
